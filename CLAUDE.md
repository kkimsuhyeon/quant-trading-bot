# CLAUDE.md — Quant Trading Bot (Crypto)

> 이 문서는 Claude Code가 매 세션 자동으로 읽는 **가드레일 문서**다.
> 코드만 봐서는 알 수 없는 "결정·원칙·방향"을 담는다. 상세 로드맵은 PLAN.md 참조.
> Codex와 Claude의 공통 협업 규칙은 `.agents/collaboration.md`를 따른다.

## 1. 이 프로젝트가 뭔가

암호화폐 **퀀트 트레이딩 시스템 (알고리즘 트레이딩)**.

- "돈 버는 봇"이 아니라 **"전략을 싸게 검증하는 실험실"을 먼저 만든다.**
- 대부분의 전략 아이디어는 검증 단계에서 탈락하는 게 정상이며, 그것이 이 시스템의 목적이다.
  (컴퓨터 위에서 싸게 죽이는 것 > 실거래로 비싸게 깨닫는 것)
- AI가 매매를 판단하는 "AI 자동매매봇"이 **아니다.** AI는 개발 도구이자 보조 특징 생성기로만 쓴다.

## 2. 대상 / 시간 단위

- **자산**: 크립토 **현물 우선** (BTC/USDT, ETH/USDT). 선물은 나중 Phase(확장)로만.
- **시간 단위**: **1시간봉 / 4시간봉** 중심 (스윙~단타). 분봉(수수료·노이즈 지옥)·일봉(표본 부족)은 지양.
- **거래소/라이브러리**: ccxt (현물·선물 동일 라이브러리). 바이낸스 우선.

## 3. 절대 가드레일 (이 선을 넘는 변경은 먼저 사람과 합의)

1. **검증 없는 실거래 절대 금지.** 어떤 전략도 다음을 통과하기 전엔 실제 돈에 닿지 않는다:
   수수료·슬리피지 반영 백테스트 → 아웃오브샘플/워크포워드 → 테스트넷 페이퍼 트레이딩.
2. **단계 순서 강제** (PLAN.md의 깔때기). 단계를 건너뛰지 않는다.
3. **하네스/전략 분리.** 백테스트 엔진(하네스)은 고정, 전략은 갈아끼우는 부품(어댑터).
   전략 추가 시 하네스 로직을 수정하지 않는다. (헥사고날 포트/어댑터 사고)
4. **룩어헤드 편향 금지.** 신호는 t-1 시점까지의 *확정된* 데이터로만 계산한다.
   아직 안 끝난 캔들의 종가나 미래 데이터로 매매를 결정하지 않는다.
5. **리스크 관리는 전략보다 우선.** 모든 전략은 포지션 크기·손절·최대 손실 한도(킬스위치)를
   가져야 한다. 시그널만 있고 리스크 규칙 없는 전략은 미완성으로 본다.
   (선물 확장 시 이 원칙은 특히 강제 — 레버리지·청산 위험 때문)
6. **AI는 트레이더가 아니라 도구다.**
   - 허용: 하네스/전략 스캐폴딩 생성, 디버깅(특히 룩어헤드 점검), 결과 해석.
   - 허용: 뉴스·심리 텍스트 → *점수(feature) 하나*로 변환 (미리 계산해 컬럼으로 저장).
   - 금지: "AI가 매수/매도 결정" (비결정적·백테스트 불가).
   - sentiment 등 AI 특징을 더한 효과는 **반드시 baseline과 백테스트 비교로 검증**한다.
7. **과최적화 경계.** 파라미터를 과거 데이터에 맞춰 깎지 않는다.
   파라미터를 살짝 바꿔도 결과가 견고한지 항상 확인. 화려한 지표 조합보다 단순+견고를 선호.
8. **전략 결합은 "포트폴리오 방식"으로만.**
   - 검증을 통과한 *독립 전략 여럿*을 **동시에 실행**하고 자본을 나눠 배분한다.
   - **장세를 예측해 전략을 갈아끼우지 않는다** (regime-switching 금지).
     각 전략이 자기 장세가 오면 알아서 수익을 내게 둔다 — "지금 추세장/횡보장"을 맞히려 하지 않는다.
   - 한 전략 *안에* 지표를 잔뜩 쌓지 않는다 (이건 과최적화. 가드레일 7번과 동일).
   - 결합은 **개별 검증을 통과한 전략만**, 나중 Phase에서.

## 4. "자리만 만들고 구현은 나중에" 원칙

다음은 *들어올 자리(인터페이스)*만 지금 설계하고, 구현은 지정된 Phase에서 한다:

- **sentiment (AI 뉴스/심리 점수)**: 데이터에 컬럼 자리 + 전략에 on/off 스위치만 둔다.
  구현은 **Phase 5**. baseline이 먼저다.
- **포트폴리오(전략 결합)**: 전략이 독립 부품으로 분리돼 있게 구조만 유지.
  실제 결합·자본 배분은 **Phase 7**, 개별 검증 완료가 전제.
- **선물 (롱/숏·레버리지)**: 확장 가능하게 구조만. 구현은 **Phase 7**, 현물 검증 완료가 전제.

## 5. 기술 스택

- 리서치·백테스트: **Python** (ccxt + pandas + Backtesting.py). 빠른 탐색이 핵심.
- 주피터 노트북으로 실험·시각화.
- (먼 미래) 라이브 엔진은 견고성 필요 시 별도 검토.

## 6. 작업 방식 (문서 규율)

- 모든 가설은 백테스트 *전에* `notes/hypothesis_*.md`에 적는다:
  "시장에 대한 믿음 한 줄 + 정확한 진입/청산 규칙 + 이 전략을 버릴 기준".
- 실험 결과·해석은 `research/` 노트북에 기록 (사후 합리화·자기기만 방지).
- 실거래 진입 *직전에만* `notes/trading_plan.md` 작성 (진입/청산/포지션 크기/손실 한도/킬스위치).
- 초보 솔로 프로젝트이므로 과잉 문서화 금지. 위 3개면 충분.

## 7. 핵심 규율

- **한 Phase를 완전히 끝내고 다음으로.** "다 하다 만"(여러 개 시작, 하나도 못 끝냄) 패턴 경계.
- 막히면 더 단순하게. 단순+검증된 것이 복잡+미검증보다 항상 낫다.

## 8. 작업 진행 방식 (이 프로젝트 오너에 대해)

- 오너는 백엔드 개발자(Java/Spring 주력)이고 파이썬은 익숙하지 않다. 퀀트는 처음이다.
- 큰 단위로 한 번에 짜지 말 것. 작은 단위로 진행하고, 사용자 결정이 필요한 지점에서만 멈춰서 확인한다.
- 코드를 한 줄씩 이해하며 갈 수 있도록 돕는다.

## 9. 에이전트 협업

- 작업 시작 전 tmux 세션 `trader-codex`와 협의한다.
- 결론이 나지 않거나 사용자 결정이 꼭 필요한 경우에만 사용자에게 묻는다.
- 상세 기준은 `.agents/collaboration.md`를 따른다.

## 10. 첫 작업

PLAN.md의 Phase 0 → Phase 1을 따른다:
ccxt로 BTC/USDT 1h 캔들 수집 → Backtesting.py 하네스 구축 →
가장 단순한 전략(SMA 교차) 하나로 파이프라인 끝까지 관통 (= hello world).
이때 sentiment 컬럼 자리와 on/off 스위치도 함께 뚫어둔다 (구현은 안 함).
