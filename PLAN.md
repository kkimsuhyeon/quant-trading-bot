# PLAN.md — Quant Trading Bot 개발 로드맵

## 프로젝트 개요

암호화폐 퀀트 트레이딩 시스템. 전략을 정의하고 → 백테스트로 검증하고 →
견고성을 확인하고 → 페이퍼 트레이딩 → 소액 실거래 → 확장하는 **검증 깔때기**를 따른다.

> 핵심 철학: 이 시스템의 목적은 "돈 버는 전략을 쌓는 것"이 아니라
> "수많은 아이디어를 단계별로 걸러 살아남는 극소수만 실제 돈에 도달시키는 것".
> **대부분의 아이디어는 탈락하는 게 정상이다.**

## 기본 정보

- **언어**: Python
- **라이브러리**: ccxt(거래소), pandas(데이터), Backtesting.py(백테스트), (선택) pandas-ta(지표)
- **자산**: 크립토 현물 우선 — BTC/USDT, ETH/USDT
- **시간 단위**: 1h / 4h 중심
- **거래소**: 바이낸스 (테스트넷 활용)

## 핵심 용어

- **하네스(harness)**: 전략을 꽂아 돌리는 *고정된 백테스트 실행 틀*. 데이터 먹이기·전략 호출·
  체결(수수료/슬리피지)·계좌 추적·결과 집계를 담당. 한 번 만들고 고정, 전략만 갈아 끼운다.
  (Backtesting.py를 우리 데이터·설정에 연결하는 얇은 코드. 밑바닥부터 짜지 않는다.)
- **전략(strategy)**: 하네스에 끼우는 *부품*. "지표 + 진입/청산 규칙"의 한 묶음.
  (지표만으로는 전략이 아니다. 예: 볼린저 + "하단 터치 시 매수" = 평균회귀 전략)
- **포트폴리오**: 검증 통과한 전략 여럿을 *동시에 실행*하며 자본을 나눠 배분한 것.
  장세를 예측해 갈아끼우는 게 아니라, 각 전략이 자기 장세에서 알아서 벌게 둔다. 결과적으로
  수익곡선이 안정화된다(분산 효과). → Phase 7.

---

## 개발 단계 (검증 깔때기)

### Phase 0: 환경 + 데이터
- [ ] Python 환경 세팅 (venv, ccxt, pandas, Backtesting.py 설치)
- [ ] ccxt로 바이낸스 BTC/USDT 1h·4h 캔들 수집
- [ ] OHLCV DataFrame 정리, CSV/parquet로 저장
- [ ] **sentiment 컬럼 자리 미리 생성** (지금은 0.0/NaN, 구현 X)

### Phase 1: 하네스 + Hello World
- [ ] Backtesting.py 하네스 구성 (수수료 파라미터 포함) — "한 번 만들고 고정"
- [ ] 가장 단순한 전략(SMA 교차) 하나로 파이프라인 끝까지 관통
- [ ] 수익률·MDD·샤프 등 지표가 출력되는지 확인
- [ ] 전략에 **sentiment on/off 스위치(`use_sentiment=False`)** 자리 설계

> 여기까지가 "실험실 완성". 이후 전략은 갈아끼우는 부품이 된다.

### Phase 2: baseline 전략 비교
- [ ] 추세추종(MA 교차/돌파) 전략 구현
- [ ] 평균회귀(볼린저/RSI) 전략 구현
- [ ] **같은 데이터에 둘 다 돌려 비교** (수수료·슬리피지 반영)
- [ ] 수익곡선·MDD·승률·샤프로 "장세에 따라 정반대로 죽는 것" 확인
- [ ] 각 전략은 *독립 부품*으로 분리 유지 (나중에 포트폴리오로 묶기 위함)

> 목표는 "이긴 전략 찾기"가 아니라 "각 전략이 어디서 깨지는지" 이해하기.

### Phase 3: 견고성 검증 (대부분 여기서 탈락)
- [ ] 아웃오브샘플(OOS): 앞 70%로 만들고 뒤 30%에서 검증
- [ ] 워크포워드 분석
- [ ] 파라미터 민감도 점검 (살짝 바꿔도 견고한가)
- [ ] 룩어헤드 편향 재점검

> 통과 못 하면 전략 폐기. 이게 정상이고 이 단계의 목적이다.

### Phase 4: 페이퍼 트레이딩
- [ ] 바이낸스 테스트넷 연결
- [ ] 검증 통과 전략을 실시간으로 페이퍼 실행 (돈 X)
- [ ] 백테스트 대비 실제 체결/슬리피지 차이 측정
- [ ] 버그·과대평가 발견

### Phase 5: AI sentiment 레이어 (자리에 구현)
- [ ] 뉴스/소셜 텍스트 수집
- [ ] AI로 텍스트 → 점수(feature) 변환, **미리 계산해 컬럼 저장** (결정적)
- [ ] point-in-time 정합성 확보 (데이터 누수 방지 — 과거 시점엔 그 시점 뉴스만)
- [ ] `use_sentiment=True/False` 비교로 **"AI를 더한 효과" 검증**

> AI는 판단자가 아니라 입력값 하나. 효과 없으면 버린다.

### Phase 6: 소액 실거래 (현물)
- [ ] `notes/trading_plan.md` 작성 (진입/청산/포지션 크기/손실 한도/킬스위치)
- [ ] 잃어도 괜찮은 최소 금액으로 시작
- [ ] 심리·실제 체결 검증 (수익이 목적 아님)

### Phase 7: 확장
- [ ] **포트폴리오 결합** — 검증 통과한 독립 전략들을 동시에 실행, 자본 분산 배분.
      장세 예측으로 갈아끼우지 않음(regime-switching 금지). 상관 낮은 전략끼리 묶어 수익곡선 안정화.
- [ ] **선물(롱/숏)** — 현물 검증 완료가 전제. 리스크 레이어 필수:
      포지션 크기·청산가 계산·레버리지 제한·손절·킬스위치
- [ ] 펀딩비 고려
- [ ] 엣지 소멸(decay) 모니터링 — 죽으면 끈다

---

## 폴더 구조

```
quant-trading-bot/
├─ CLAUDE.md                  세션 가드레일 (Claude Code 자동 로드)
├─ PLAN.md                    이 문서
├─ data/                      캔들 데이터 (+ 나중에 sentiment)
├─ strategies/                전략 = 갈아끼우는 부품 (독립 유지)
│   ├─ sma_cross.py
│   ├─ trend.py
│   └─ mean_revert.py
├─ research/                  주피터 노트북 = 연구일지
│   └─ YYYY-MM-DD_experiment.ipynb
├─ notes/
│   ├─ hypothesis_*.md        가설 문서 (백테스트 전에 작성)
│   └─ trading_plan.md        실거래 규칙서 (실거래 직전에만)
├─ backtest.py                하네스 (만들고 고정)
└─ requirements.txt
```

## 핵심 규율

- 한 Phase를 완전히 끝내고 다음으로. "다 하다 만" 패턴 경계.
- 가설과 "버릴 기준"은 백테스트 *전에* 적는다 (사후 합리화 방지).
- 전략은 독립 부품으로. 결합은 검증 통과한 것만, Phase 7에서.
- 단순+검증 > 복잡+미검증. 막히면 더 단순하게.
